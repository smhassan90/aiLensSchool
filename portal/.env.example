# Local portal dev → VPS backend (recommended)
NEXT_PUBLIC_API_URL=https://hawknexabackend.fynals.com/api/v1
# Leave NEXT_PUBLIC_USE_LOCAL_API unset (or false) so the browser calls the VPS API.

# Optional: local portal + local Nest API
# NEXT_PUBLIC_USE_LOCAL_API=true
# NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
